model:
  base: openai/privacy-filter  # opf only accepts its own checkpoint format; OpenMed (221 labels) not in known set (v2:33, v4:57, v7:101)

training:
  learning_rate: 1.0e-4
  epochs: 3
  batch_size: 2
  grad_accumulation_steps: 4
  dtype: bf16
  weight_decay: 0.0

dataset:
  hf_path: msdakot/fintech-privacy-pii

output:
  hf_repo: msdakot/fintech-privacy-filter-v0
  checkpoint_dir: /content/drive/MyDrive/fintech-pii-checkpoints  # Colab Drive path
